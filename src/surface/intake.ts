/**
 * Intake: the surface module for capturing intention.
 * Stub for staged migration.
 */

export interface IntakeOptions {
  // Placeholder
}

export async function mountIntake(options: IntakeOptions): Promise<void> {
  // Placeholder
}
